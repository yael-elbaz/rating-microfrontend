{
  "name": "rating-microfrontend",
  "private": true,
  "workspaces": [
    "packages/*",
    "apps/*"
  ],
  "scripts": {
    "build:utils": "npm run build -w digital-utils",
    "start:host": "npm run start -w host",
    "start:mfe": "npm run start -w mfe-example",
    "start": "npm run build:utils && concurrently -n mfe,host \"npm run start:mfe\" \"npm run start:host\"",
    "build": "npm run build:utils && npm run build -w mfe-example && npm run build -w host"
  },
  "devDependencies": {
    "concurrently": "^9.1.0"
  }
}
